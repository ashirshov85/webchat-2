# Feature Specification: SSO — вход через внешние identity-провайдеры (OIDC/OAuth2)

**Feature Branch**: `003-sso-identity-providers`

**Created**: 2026-09-16

**Status**: Approved

**Input**: User description: "Вход через внешние identity-провайдеры по OIDC/OAuth2 как равноправная альтернатива паролю. Пользователь может привязать внешний IdP-аккаунт к существующему аккаунту и входить любым из способов. Поддержка конфигурации нескольких провайдеров (endpoints, client id/secret в secret manager), автоматическое создание аккаунта при первом входе через IdP при совпадении email. Единая модель сессий/токенов с фичей 002."

## Clarifications

### Session 2026-09-20

- **Постановка**: экран входа (`/login`) переводится на визуальный стиль
  дизайн-прототипа (`tmp/login.html` + `tmp/bg2.png`; каталог `tmp/` не
  версионируется — нормативный инвентарь зафиксирован в research §22):
  полноэкранный фон с затемнением, центральная «латунная» карточка, шрифты
  Cinzel/EB Garamond, золотая палитра, заклёпки, шестерёнка-бейдж, поля с
  иконками, кнопка показа пароля, SSO-кнопки «Продолжить с …». US7, Phase 10
  в tasks.md; изменение презентационное — API-контракт и поведение не меняются.
- Q: Язык интерфейса экрана входа? → A: Русский, как в прототипе («ВОЙТИ»,
  «Продолжить с …», «Нет аккаунта? Зарегистрироваться»); тексты в автотестах
  обновляются синхронно (Test-First).
- Q: Что делать с глобальным заголовком `<h1>WebChat</h1>` над страницей? → A:
  Скрывается только на `/login` (бренд уже в карточке — STEAMCHAT + бейдж);
  на остальных маршрутах остаётся без изменений.
- Q: Как подключать шрифты Cinzel/EB Garamond? → A: Self-host — пакеты
  `@fontsource/cinzel`, `@fontsource/eb-garamond`; внешние CDN не используются.
  Фоновое изображение — `frontend/public/bg2.png` (копия из `tmp/`).
- Q: Объём редизайна? → A: `LoginPage` (US7) и `RegisterPage` (US8) одной
  итерацией: общий визуальный язык, общий файл стилей `auth-theme.css`
  (research §22.6). Остальные auth-страницы (confirm-registration,
  set-password, forgot/reset-password, sso/callback) — вне scope, при
  необходимости отдельная итерация.

### Session 2026-09-19

- **Ревизия Assumption про «только OIDC»**: при подключении Яндекса на dev-стенде
  выяснено, что Яндекс ID не поддерживает OIDC (только OAuth 2.0 + API профиля);
  GitHub — аналогично. Ранее «чистый OAuth2 не поддерживается — YAGNI» снимается:
  вводится режим `oauth2-userinfo` (research §16–§21 дополнения 2026-09-19);
  конституция V требует «SSO (OIDC/OAuth2)». Публичный API-контракт не меняется.

### Session 2026-09-16

- Q: Что происходит с уже выданными сессиями при отвязке внешнего провайдера? → A: Сессии не отзываются — отвязка блокирует только новые входы, активные сессии работают до выхода/истечения.
- Q: Какой режим доверия по умолчанию для автосвязывания по email? → A: Opt-in allowlist — автосвязывание с существующим аккаунтом только для явно доверенных провайдеров; остальные — JIT и ручная привязка.
- Q: Требовать ли подтверждение (ввод пароля) при первой автопривязке к существующему аккаунту? → A: Нет — allowlist + verified email достаточно, подтверждение не запрашивается.
- Q: Как назначается username при JIT-создании аккаунта? → A: Автогенерация системой из email с гарантией уникальности, без участия пользователя.

## User Scenarios & Testing *(mandatory)*

### User Story 1 - Вход через внешнего провайдера с единой сессией (Priority: P1)

Пользователь с привязанной внешней идентичностью входит без пароля: на экране входа он выбирает одного из включённых провайдеров, аутентифицируется на стороне провайдера и возвращается в чат уже вошедшим. Система выдаёт ту же пару токенов (короткоживущий токен сессии + refresh-токен), что и при входе по паролю: продление по refresh с ротацией, выход с отзывом обоих токенов и все остальные свойства сессий фичи 002 работают одинаково. Способ входа не влияет на права и доступ к API: сессия, созданная через IdP, равноправна парольной.

**Why this priority**: Это ядро фичи — равноправная альтернатива паролю; без работающего входа через провайдера остальные сценарии (JIT, привязки) не имеют смысла.

**Independent Test**: Автотестом полного флоу с эмулируемым провайдером: старт входа через провайдера → аутентификация → возврат → получена пара токенов, идентичная по свойствам парольной; продление по refresh ротирует токен; logout отзывает оба токена; доступ к защищённому API работает по токену сессии.

**Acceptance Scenarios**:

1. **Given** на экране входа отображаются включённые провайдеры, **When** пользователь выбирает провайдера и успешно аутентифицируется у него, **Then** он возвращается в приложение вошедшим: создаётся сессия и выдаётся пара токенов того же формата и с теми же свойствами, что при парольном входе.
2. **Given** сессия создана через провайдера, **When** срок токена сессии истекает и клиент продлевает её по refresh-токену, **Then** продление работает так же, как для парольного входа, включая ротацию refresh-токена.
3. **Given** сессия создана через провайдера, **When** пользователь выполняет выход, **Then** токен сессии и refresh-токен отзываются, дальнейшее их использование отклоняется.
4. **Given** у пользователя есть и пароль, и привязанная внешняя идентичность, **When** он входит любым из способов, **Then** обе сессии дают одинаковый доступ ко всем API чата.
5. **Given** пользователь отменяет согласие у провайдера или провайдер возвращает ошибку, **When** происходит возврат в приложение, **Then** вход не выполняется, показывается понятная ошибка, состояние аккаунта не меняется.
6. **Given** пользователь инициирует вход с экрана глубже корневого (задан `returnTo`), **When** вход через провайдера завершается успешно, **Then** после обмена handshake-кода на сессию клиент возвращает пользователя по указанному относительному пути; не-относительный `returnTo` отклоняется на этапе authorize (400).

---

### User Story 2 - Первый вход через IdP: автосвязывание или автоматическое создание аккаунта (Priority: P1)

Пользователь, у которого ещё нет внешних привязок, впервые входит через провайдера. Если провайдер подтверждает email пользователя, входит в allowlist доверенных и в системе есть активный аккаунт с таким email, внешняя идентичность автоматически привязывается к этому аккаунту и вход выполняется в него — без запроса пароля. Если аккаунта с таким email нет, система автоматически создаёт активный аккаунт (JIT): email берётся от провайдера, уникальный username генерируется системой, внешняя идентичность сразу привязывается — пользователь попадает в чат без ручной регистрации, писем и задания пароля. Если провайдер не подтверждает email, не возвращает его либо (при совпадении с существующим аккаунтом) не входит в allowlist доверенных, автоматическое связывание не выполняется.

**Why this priority**: Автоматическое создание/связывание по email — явно заявленная ценность фичи (бесшовный первый вход); вместе с US1 образует минимальный жизнеспособный сценарий.

**Independent Test**: Автотестом с эмулируемым провайдером: первый вход с подтверждённым email, отсутствующим в системе, → создан активный аккаунт с привязкой; первый вход с подтверждённым email существующего активного аккаунта → вход выполнен в этот аккаунт, привязка добавлена; вход с неподтверждённым email → ничего не создаётся и не связывается.

**Acceptance Scenarios**:

1. **Given** в системе нет аккаунта с email E и провайдер подтверждает email E, **When** пользователь впервые входит через этого провайдера, **Then** автоматически создаётся активный аккаунт с email E и системой-сгенерированным уникальным username, внешняя идентичность привязывается к нему, вход выполняется; ручные шаги регистрации не требуются.
2. **Given** существует активный аккаунт с email E (созданный по паролю в фиче 002), провайдер подтверждает email E и входит в allowlist доверенных, **When** пользователь впервые входит через этого провайдера, **Then** внешняя идентичность автоматически привязывается к существующему аккаунту и вход выполняется именно в него; новый аккаунт не создаётся.
3. **Given** провайдер не подтверждает email, не возвращает его, либо email совпадает с существующим активным аккаунтом, а провайдер не входит в allowlist доверенных, **When** пользователь пытается войти впервые (внешняя идентичность не привязана ни к одному аккаунту), **Then** автоматическое связывание не выполняется, ничего не создаётся; показывается понятное сообщение с предложением войти по паролю (или зарегистрироваться, если аккаунта нет) и привязать провайдера в настройках (FR-004).
4. **Given** существует не завершивший регистрацию аккаунт (статус `pending` или `awaiting_password`) с email E, **When** пользователь впервые входит через провайдера с подтверждённым email E, **Then** автосвязывание и JIT-создание не выполняются (дубль email невозможен, активация в обход задания пароля недопустима); пользователю предлагается завершить регистрацию по ссылке из письма (фича 002).
5. **Given** JIT-аккаунт создан без пароля, **When** выполняется попытка входа по паролю для него, **Then** вход отклоняется; пользователь может задать пароль через механизм восстановления пароля из фичи 002, после чего оба способа входа доступны.
6. **Given** сгенерированный username конфликтует с существующим, **When** создаётся JIT-аккаунт, **Then** система подбирает другой уникальный username; для пользователя это незаметно.
7. **Given** провайдер вне allowlist доверенных, email подтверждён, аккаунта с таким email в системе нет, **When** пользователь впервые входит через него, **Then** выполняется JIT-создание аккаунта и вход — allowlist требуется только для автосвязывания, не для JIT (FR-004).

---

### User Story 3 - Ручное управление привязками внешних IdP (Priority: P2)

Аутентифицированный пользователь управляет внешними привязками своего аккаунта: инициирует привязку дополнительного провайдера (проходя полный флоу аутентификации у провайдера, чем доказывает контроль над внешним аккаунтом) и отвязывает ненужные. Список подключённых провайдеров с их email отображается в настройках безопасности. Отвязка разрешена только если у аккаунта остаётся хотя бы один способ входа (пароль или другая привязка) — система не даёт пользователю заблокировать себя. Email аккаунта при привязке/отвязке не меняется.

**Why this priority**: Ручная привязка нужна, когда email аккаунта и провайдера различаются (автосвязывание не сработает); не блокирует вход через уже привязанных провайдеров, поэтому P2.

**Independent Test**: Автотестом API: вошедший пользователь привязывает провайдера → идентичность появляется в списке привязок и становится способом входа; отвязка → вход через этого провайдера отклоняется; попытка отвязать последний способ входа → отклоняется.

**Acceptance Scenarios**:

1. **Given** аутентифицированный пользователь, **When** он инициирует привязку провайдера и успешно аутентифицируется у него, **Then** внешняя идентичность привязывается к его аккаунту и отображается в списке подключённых; вход через этого провайдера становится возможен.
2. **Given** внешняя идентичность (провайдер + идентификатор пользователя у провайдера) уже принадлежит другому аккаунту, **When** пользователь пытается привязать её, **Then** привязка отклоняется с понятной ошибкой, без раскрытия деталей чужого аккаунта.
3. **Given** у аккаунта есть пароль и одна внешняя привязка, **When** пользователь отвязывает провайдера, **Then** привязка удаляется, вход по паролю остаётся доступен; повторный вход через эту внешнюю идентичность обрабатывается по правилам первого входа (FR-003/FR-004): провайдер вне allowlist — отказ `email_conflict`, trusted-провайдер с совпадающим подтверждённым email — повторная автопривязка и вход.
4. **Given** у аккаунта нет пароля (JIT-аккаунт) и осталась единственная внешняя привязка, **When** пользователь пытается её отвязать, **Then** отвязка отклоняется — нельзя удалить последний доступный способ входа; перед этим пользователю предлагается сначала задать пароль.
5. **Given** привязываемый провайдер возвращает email, отличный от email аккаунта, **When** пользователь выполняет привязку, **Then** привязка разрешается (контроль над обеими сторонами подтверждён), email аккаунта не изменяется.
6. **Given** у пользователя есть активные сессии, включая созданные через отвязываемого провайдера, **When** отвязка выполняется, **Then** существующие сессии не отзываются и продолжают работать до выхода или истечения срока; блокируется только вход через отвязанного провайдера для новых входов.

---

### User Story 4 - Конфигурация нескольких провайдеров, секреты в secret manager (Priority: P2)

Система одновременно поддерживает несколько включённых внешних провайдеров: каждый задаётся конфигурацией (отображаемое имя, endpoints, client id, признак включённости), а client secret и прочие секретные значения хранятся исключительно в secret manager и никогда — в коде или репозитории. Экран входа перечисляет всех включённых провайдеров; выключение провайдера убирает его из перечня, не затрагивая остальные способы входа и уже существующие привязки. Ошибка конфигурации одного провайдера не влияет на вход через остальных и по паролю.

**Why this priority**: Возможность нескольких провайдеров — заявленное требование, но сам вход (US1–US2) проверяем и с одним настроенным провайдером; конфигурационная механика — P2.

**Independent Test**: Автотестом/аудитом: при ≥2 настроенных провайдерах экран входа перечисляет обоих и вход работает через каждого; секрет провайдера отсутствует в коде, конфигах репозитория и логах; при выключении провайдера он исчезает из перечня и его флоу отклоняется.

**Acceptance Scenarios**:

1. **Given** настроены и включены два и более провайдеров, **When** пользователь открывает экран входа, **Then** отображаются все включённые провайдеры, и вход возможен через каждого из них.
2. **Given** провайдер сконфигурирован, **When** система обращается к нему, **Then** client secret и другие секреты читаются из secret manager и нигде не встречаются в коде, репозитории и логах.
3. **Given** провайдер выключен, **When** пользователь открывает экран входа, **Then** провайдер не отображается; прямая попытка начать его флоу отклоняется; существующие привязки и остальные способы входа не затронуты.
4. **Given** у провайдера неверный secret, недоступные endpoints (ошибка соединения/таймаут) или отклонение обмена кода (неверный/отозванный client id или secret), **When** выполняется попытка входа через него, **Then** пользователь получает понятную ошибку, сбой фиксируется в observability, вход через остальных провайдеров и по паролю продолжает работать.

---

### User Story 5 - Защита SSO-флоу и устойчивость при сбоях провайдера (Priority: P2)

Публичные endpoints SSO-флоу защищены rate limiting'ом по источнику запроса (консистентно при горизонтальном масштабировании, как в фиче 002). Диалог с провайдером защищён от подделки межсайтовых запросов и повторного использования ответов провайдера: подделанный или повторённый ответ отклоняется, сессия не создаётся. Недоступность провайдера обрабатывается грациозно: пользователь получает понятную ошибку за разумное время, парольный вход и остальные провайдеры не деградируют, сбой виден операторам. Все события SSO (входы, привязки, отвязки, JIT-создания, ошибки) фиксируются в журнале безопасности без секретов.

**Why this priority**: Прямое требование безопасности конституции к публичным endpoints и SSO; защищает сценарии US1–US4, проверяется независимо от деталей конкретных провайдеров.

**Independent Test**: Автотестом: серия запросов к SSO-endpoints сверх лимита отклоняется; callback с изменённым/повторённым параметром защиты отклоняется без создания сессии; при недоступном провайдере (таймаут) вход завершается понятной ошибкой, парольный вход работает, событие фиксируется в журнале/метриках.

**Acceptance Scenarios**:

1. **Given** публичные endpoints SSO-флоу, **When** количество запросов от одного источника превышает лимит, **Then** избыточные запросы отклоняются с сообщением о лимите; учёт лимитов общий для всех экземпляров системы.
2. **Given** ответ провайдера подделан или воспроизведён повторно (проверка целостности флоу не проходит), **When** выполняется завершение входа, **Then** запрос отклоняется, сессия и привязка не создаются.
3. **Given** провайдер недоступен или отвечает с ошибкой, **When** пользователь пытается войти через него, **Then** в пределах разумного времени возвращается понятная ошибка без системного сбоя; вход по паролю и через остальных провайдеров работает; сбой наблюдаем в метриках/логах.
4. **Given** происходит любое событие SSO (успешный/неуспешный вход, привязка, отвязка, JIT-создание), **When** событие завершается, **Then** оно фиксируется в журнале событий безопасности без записи секретов и токенов в открытом виде.

---

### User Story 6 - Вход через OAuth2-провайдера без OIDC (Яндекс) (Priority: P1)

Система поддерживает провайдеров, реализующих только OAuth 2.0 без OpenID-идентичности (без `id_token`): после обмена кода на access-токен система запрашивает профиль пользователя на userinfo-эндпоинте провайдера и использует его утверждения (идентификатор, подтверждённый email) ровно тем же образом, что и клеймы ID-токена. Для пользователя вход через такого провайдера неотличим от входа через OIDC-провайдера: единая сессия, JIT-создание, автосвязывание, ручные привязки, защита флоу и обработка отказов работают идентично.

**Why this priority**: целевой провайдер стенда (Яндекс) — OAuth2-only; без этого режима вход через него невозможен (текущее состояние: 502 «provider temporarily unavailable»).

**Independent Test**: Автотестом (MockIdP в oauth2-режиме, без `id_token`): полный вход через oauth2-провайдера создаёт единую сессию; первый вход с подтверждённым email создаёт JIT-аккаунт; недоступность userinfo/отсутствие subject-клейма/неверный secret → `provider_error`, остальные провайдеры и парольный вход не затронуты.

**Acceptance Scenarios**:

1. **Given** настроен включённый провайдер с `protocol: oauth2-userinfo` (Яндекс), **When** пользователь проходит его флоу входа, **Then** система обменивает код, запрашивает профиль backchannel'ом, резолвит идентичность и выдаёт сессию по единой модели (US1); экран входа и SPA не отличаются от OIDC-провайдера.
2. **Given** первый вход через oauth2-провайдера с подтверждённым email, **When** профиль получен, **Then** действует решающая матрица US2 без изменений (JIT/автосвязывание по allowlist/`registration_incomplete`).
3. **Given** userinfo-эндпоинт недоступен, отвечает ошибкой, профиль не содержит subject-клейма, либо обмен кода отвергнут (неверный secret), **When** завершается флоу, **Then** пользователь получает `provider_error` ≤5 с, сбой наблюдаем, вход через остальных провайдеров и по паролю работает (US5-3/US5-4).
4. **Given** провайдер не поддерживает PKCE (`pkce: false`), **When** выполняется флоу, **Then** код обменивается с client_secret по TLS; защита флоу state/single-use сохраняется.

---

### User Story 7 - Экран входа в визуальном стиле дизайн-прототипа (Priority: P2)

Пользователь открывает `/login` и видит экран входа, визуально соответствующий дизайн-прототипу: полноэкранный фон с радиальным затемнением, центральная «латунная» карточка (золотая палитра, шрифты Cinzel/EB Garamond, заклёпки по углам, шестерёнка-бейдж с иконкой чата), поля логина/пароля с иконками внутри, кнопка показа пароля, золотая градиентная кнопка входа, разделитель «ИЛИ», SSO-кнопки «Продолжить с <Провайдер>» с брендовыми иконками и ссылка на регистрацию. Вся функциональность US1–US6 на этом экране (парольный вход, перечень включённых провайдеров, старт флоу, `returnTo`, ошибки `problem+json`, welcome-сообщение) сохраняется без изменений — меняется только презентация.

**Why this priority**: чисто презентационная история поверх работающего входа (US1); не блокирует ни один функциональный сценарий и не влияет на API.

**Independent Test**: Обновлённые Vitest/RTL-тесты `LoginPage.test.tsx` / `LoginPageSso.test.tsx` на русском тексте интерфейса: форма рендерится, submit вызывает login, ошибки отображаются в `role=alert`, кнопки провайдеров рендерятся из мокнутого `GET /auth/sso/providers`, клик → redirect по `authorizationUrl`, `returnTo` сохраняется. Ручная визуальная проверка — quickstart S9.

**Acceptance Scenarios**:

1. **Given** пользователь открывает `/login`, **When** страница загружена, **Then** экран визуально соответствует прототипу (research §22): полноэкранный фон `bg2.png` с затемнением, карточка с золотой рамкой и тенями, заголовок STEAMCHAT шрифтом Cinzel, подпись CONNECT·CHAT·EXPLORE, заклёпки в углах, шестерёнка-бейдж над карточкой.
2. **Given** заполнены идентификатор и пароль, **When** пользователь нажимает «ВОЙТИ», **Then** выполняется тот же `POST /auth/login`, что и до редизайна; ошибки показываются в `role=alert` внутри карточки, успешный вход — в `role=status` (welcome).
3. **Given** поле пароля, **When** пользователь нажимает кнопку-глаз, **Then** пароль показывается/скрывается, `aria-label` кнопки переключается («Показать пароль»/«Скрыть пароль»); фокус и ввод не сбрасываются.
4. **Given** включено ≥1 провайдер, **When** рендерится SSO-блок, **Then** отображаются только провайдеры из `GET /auth/sso/providers` в порядке конфигурации, с брендовой иконкой по `provider.id` (google/github/yandex/vk/dex) и generic-иконкой для остальных; клик выполняет тот же authorize-флоу с `returnTo` (US1) без изменений.
5. **Given** провайдеры не настроены, **When** перечень пуст, **Then** разделитель «ИЛИ» и SSO-блок не отображаются; карточка не «ломается».
6. **Given** пользователь на `/login` или `/register`, **When** страница отрисована, **Then** глобальный заголовок `<h1>WebChat</h1>` скрыт на этих маршрутах (на `/register` — совместно с US8); на всех остальных маршрутах он отображается как раньше.
7. **Given** любые страницы приложения вне US7/US8, **When** применяется редизайн, **Then** их стили не изменяются: новые стили scoped в `auth-theme.css`, глобальный `index.css` не расширяется (кроме скрытия h1 на `/login` и `/register` на уровне разметки `App.tsx`).
8. **Given** системная настройка prefers-reduced-motion, **When** пользователь открывает страницу, **Then** анимация вращения шестерёнки отключается; декоративные элементы (`aria-hidden`) не попадают в accessibility-дерево, поля остаются доступными (sr-only label, `autoComplete` сохранены).

---

### User Story 8 - Экран регистрации в визуальном стиле дизайн-прототипа (Priority: P2)

Пользователь открывает `/register` и видит форму регистрации в том же визуальном языке, что и экран входа (US7): полноэкранный фон с затемнением, «латунная» карточка с заклёпками и шестерёнкой-бейджем, бренд-блок STEAMCHAT + CONNECT·CHAT·EXPLORE, шрифты Cinzel/EB Garamond, золотая палитра. Состав: заголовок «Создание аккаунта», поля «Имя пользователя» и «Email» с иконками, золотая градиентная кнопка «ЗАРЕГИСТРИРОВАТЬСЯ», футер «Уже есть аккаунт? Войти». Поведение регистрации не меняется: тот же `POST /auth/register`, отображение кодов ошибок 400/409 (`problem+json`) в `role=alert`, успех — сообщение о письме со ссылкой в `role=status` (research §22.6).

**Why this priority**: единый визуальный язык auth-экранов поверх работающей регистрации (фича 002); презентационная история, не блокирует ничего.

**Independent Test**: Обновлённые Vitest/RTL-тесты Register-ветки `RegisterPages.test.tsx` на русском DOM-контракте: форма рендерится, submit вызывает `register({username, email})`, ошибки полей (400) и конфликтов (409) отображаются в `role=alert`, успех — `role=status` с email. Визуальная проверка — quickstart S9.

**Acceptance Scenarios**:

1. **Given** пользователь открывает `/register`, **When** страница загружена, **Then** экран визуально соответствует US7 (фон, карточка, рамка/тени, заклёпки, бейдж, бренд-блок, шрифты, палитра) — research §22.6; карточка переиспользует общие стили `auth-theme.css` без дублирования CSS (FR-022).
2. **Given** поля формы, **When** пользователь вводит имя пользователя и email, **Then** поля оформлены как в US7 (иконка слева, placeholder + sr-only label, `autoComplete` сохранены), ввод и отправка работают как раньше — тот же `POST /auth/register` с теми же payload-параметрами.
3. **Given** регистрация завершается ошибкой валидации (400) или конфликтом (409), **When** ответ получен, **Then** коды ошибок отображаются в `role=alert` внутри карточки; при успехе — `role=status` с сообщением о письме и адресом; формат сообщений не меняется (существующий `problemMessage`).
4. **Given** отправка выполняется, **When** запрос в полёте, **Then** кнопка «ЗАРЕГИСТРИРОВАТЬСЯ» заблокирована (submitting), как «ВОЙТИ» в US7-2.
5. **Given** футер карточки, **When** пользователь переходит по «Уже есть аккаунт? Войти» → `/login`, и по «Зарегистрироваться» с `/login` → `/register` (US7), **Then** навигация между экранами работает в обе стороны.
6. **Given** пользователь на `/register`, **When** страница отрисована, **Then** глобальный `<h1>WebChat</h1>` скрыт (как на `/login`, US7-6); на остальных маршрутах отображается.
7. **Given** карточка регистрации, **When** сравнивается с login, **Then** отсутствуют divider «ИЛИ» и SSO-блок (регистрация через IdP не выполняется формой — вход через провайдера создаёт аккаунт сам, US2); их отсутствие не ломает вёрстку.

---

### Edge Cases

- Что если у аккаунта не осталось ни пароля, ни привязок? — Невозможно по построению: отвязка последнего способа входа блокируется (US3-4); пользователь без пароля (JIT) обязан оставить хотя бы одну привязку.
- Что если один и тот же внешний аккаунт пытаются привязать два пользователя? — Внешняя идентичность принадлежит не более чем одному аккаунту; вторая привязка отклоняется (US3-2). Если нужен переход между аккаунтами — сначала отвязка владельцем.
- Что если два разных провайдера возвращают один и тот же подтверждённый email? — Обе идентичности связываются/создаются на одном аккаунте с этим email (уникальность email сохраняется, FR-003).
- Что если провайдер сменил email пользователя после привязки? — Привязка идентична по идентификатору пользователя у провайдера, а не по email: вход продолжает работать в тот же аккаунт; email аккаунта не перезаписывается.
- Что если JIT-аккаунт хочет войти по паролю, но пароля нет? — Отклоняется; пароль задаётся через восстановление по email из фичи 002 (US2-5).
- Что если email у провайдера не подтверждён, либо провайдер вне allowlist и email совпадает с существующим аккаунтом? — Автосвязывание не выполняется (защита от захвата аккаунта через чужой/неподтверждённый email); пользователь входит по паролю и привязывает провайдера вручную.
- Что если во время OIDC-флоу пользователь открыл несколько вкладок/начал несколько флоу? — Каждый флоу проверяется независимо; завершение «чужого» или повторное завершение отклоняется проверкой целостности (US5-2).
- Что если провайдер возвращает email в другой нормализации (регистр, пробелы)? — Сопоставление email выполняется по канонической нормализации, как в фиче 002; дублей из-за регистра не возникает.
- Что если провайдер не возвращает идентификатор пользователя (пустой/отсутствующий `sub`)? — Такой провайдер считается некорректно настроенным: флоу отклоняется, событие фиксируется в observability.
- Что если секрет провайдера скомпрометирован/ротирован? — Секрет заменяется в secret manager без изменения кода; старые флоу завершить нельзя после смены (проверка целостности), привязки сохраняются.
- Что если во время завершения флоу (callback) система недоступна/происходит рестарт? — Операции завершения флоу идемпотентны по эффектам: повторный callback не создаёт дублей привязок или аккаунтов.
- Что если пользователь с паролем потерял доступ к внешнему аккаунту? — Использует парольный вход; внешняя привязка не мешает и может быть отвязана (если не последняя — а пароль есть).
- Что если oauth2-провайдер сменил основной email пользователя после привязки? — Как и для OIDC: идентичность определяется по subject-клейму (`psuid`), email аккаунта не перезаписывается; email привязки обновляется как «последнее известное».
- Что если userinfo-ответ не содержит email-клейм (доступ «Адрес электронной почты» не выдан приложению)? — Email-гейт FR-004: `email_not_verified`-семейство (нет подтверждённого email → вход отклонён, привязка не создаётся); оператору видна причина в observability.
- Что если провайдер объявлен `oauth2-userinfo`, но сконфигурирован с `issuer-uri`/без `userinfo-uri`? — fail-fast на старте (расширение валидации T007): включённый oauth2-провайдер обязан иметь authorization/token/userinfo URI и client-id/secret.
- Что если у провайдера нет брендовой иконки в мапе (новый/кастомный провайдер, dex)? — Рендерится generic-иконка в стиле карточки; состав кнопок и флоу входа не зависят от наличия иконки (US7-4).
- Что если фоновое изображение не загрузилось (медленная сеть)? — Карточка остаётся читаемой: под фотоном лежит тёмный цвет фона с затемнением; вёрстка не зависит от загрузки изображения.
- Что если высота вьюпорта меньше высоты карточки (мобильный landscape)? — Страница прокручивается: карточка не обрезается, бейдж-шестерёнка не перекрывается (scroll-safe центрирование).

## Requirements *(mandatory)*

### Functional Requirements

- **FR-001**: Система MUST поддерживать вход через любого включённого внешнего identity-провайдера по стандартному флоу OIDC authorization code flow с PKCE как равноправную альтернативу входу по паролю.
- **FR-002**: Успешный вход через провайдера MUST создавать сессию по единой модели фичи 002: короткоживущий токен сессии + refresh-токен с ротацией при каждом использовании, отзыв по выходу, полный отзыв при смене пароля; способ входа MUST НЕ влиять на права и доступ к API.
- **FR-003**: При первом входе через провайдера, когда условия автоматических действий (FR-004) выполнены, система MUST: при наличии активного аккаунта с совпадающим (по канонической нормализации) email — автоматически привязать внешнюю идентичность к этому аккаунту и выполнить вход в него; при отсутствии аккаунта — автоматически создать активный аккаунт (JIT) с этим email и сгенерированным уникальным username, привязать идентичность и выполнить вход; при наличии не завершившего регистрацию аккаунта (статусы `pending` или `awaiting_password` фичи 002) с совпадающим email — отказ без связывания и создания (код `registration_incomplete`, предложение завершить регистрацию по ссылке из письма — фича 002, US2-4); в случаях успеха — без ручных шагов регистрации и без дополнительного подтверждения владельцем (условия доверия — FR-004); полная решающая матрица первого входа (все комбинации условий и исходов) — data-model.md §7, нормативный источник; в spec матрица не дублируется.
- **FR-004** (условия для эффектов FR-003): Автоматическое связывание и JIT-создание MUST выполняться только при подтверждении email провайдером; автосвязывание с существующим аккаунтом — дополнительно только для trusted-провайдеров (allowlist автосвязывания; конфигурационный признак `trusted-for-email-linking`, opt-in, по умолчанию выключен), JIT-создание allowlist не требует (US2-7); во всех остальных случаях — отказ с понятным сообщением и предложением войти по паролю (или зарегистрироваться, если аккаунта нет) и привязать провайдера вручную.
- **FR-005**: Аутентифицированный пользователь MUST иметь возможность привязать к своему аккаунту внешнюю идентичность (пройдя аутентификацию у провайдера) и отвязать её; система MUST запрещать отвязку последнего доступного способа входа (пароль или внешняя привязка); отвязка MUST НЕ отзывать существующие сессии — блокируются только новые входы через отвязанного провайдера.
- **FR-006**: Внешняя идентичность (провайдер + уникальный идентификатор пользователя у провайдера) MUST принадлежать не более чем одному аккаунту; попытка привязки уже занятой идентичности MUST отклоняться без раскрытия деталей аккаунта-владельца.
- **FR-007**: Система MUST поддерживать одновременную конфигурацию нескольких провайдеров; для каждого задаются отображаемое имя, endpoints, client id и состояние (включён/выключен); client secret и другие production-секреты MUST храниться исключительно в secret manager и нигде не появляться в коде, репозитории и логах. Единственное задокументированное исключение — dev-only dummy-секрет локального IdP в `deploy/local/dex/` (Assumptions): заведомо неиспользуемое в production значение, не являющееся секретом реального контура.
- **FR-008**: Перечень включённых провайдеров MUST быть доступен на экране входа без аутентификации (порядок отображения — порядок объявления в конфигурации); публичный перечень endpoints без аутентификации расширяется соответствующими SSO-endpoints (перечень провайдеров, начало флоу, завершение флоу, обмен одноразового handshake-кода на сессию — термин и формат кода определены в contracts/sso-api.md §3–4), точный список фиксируется в API-контракте. Запрос начала флоу MUST принимать опциональный `returnTo` — относительный путь возврата внутри приложения; сервер MUST отклонять не-относительный путь и путь длиннее 512 символов (400; формат и предельная длина — contracts/sso-api.md §2), клиент применяет его после успешного обмена handshake-кода на сессию (fallback — маршрут по умолчанию).
- **FR-009**: Rate limiting по источнику запроса, консистентный при горизонтальном масштабировании (состояние во внешнем хранилище, как в фичи 002), MUST охватывать ровно пять SSO-маршрутов: публичные — перечень провайдеров, начало флоу входа, завершение флоу (callback), обмен handshake-кода на сессию — и аутентифицированное начало link-флоу; CRUD привязок (`GET/DELETE /users/me/identities`) защищается Bearer-аутентификацией, отдельные SSO-лимиты на него не вводятся (research §9); диалог с провайдером MUST быть защищён от подделки межсайтовых запросов и повторного использования ответов провайдера. Нормативные значения лимитов фиксируются в research §9 и проверяются интеграционным тестом лимитов US5 из tasks.md (Phase 7) (в spec не дублируются — как и перечни кодов sso_error).
- **FR-010**: Отказ или недоступность провайдера MUST обрабатываться грациозно: понятная пользователю ошибка в пределах разумного времени (не более 5 секунд — SC-005), отсутствие влияния на парольный вход и остальных провайдеров, фиксация сбоя в observability (логи/метрики).
- **FR-011**: Все события SSO (успешный/неуспешный вход, привязка, отвязка, JIT-создание аккаунта, ошибки флоу) MUST фиксироваться в журнале событий безопасности без секретов и токенов в открытом виде.
- **FR-012**: Операции завершения флоу (callback, привязка, JIT-создание) MUST быть идемпотентны по эффектам: повторное выполнение не создаёт дублей аккаунтов, привязок или сессий.
- **FR-013**: Для JIT-аккаунта без пароля вход по паролю MUST отклоняться; задание пароля MUST быть возможно через механизм восстановления пароля фичи 002.
- **FR-014**: Система MUST поддерживать режим `oauth2-userinfo` per-provider (research §16): authorization code flow без `id_token`, с получением профиля на userinfo-эндпоинте backchannel'ом и маппингом клеймов (`subject-claim`, `email-claim`, `email-verified-mode`) в единую модель идентичности; семантика решающей матрицы первого входа (FR-003/FR-004), привязок (FR-005/FR-006), защиты флоу (FR-009) и отказоустойчивости (FR-010) — без изменений и протокольных веток в домене.
- **FR-015**: Для `oauth2-userinfo` провайдеров граница доверия — одноразовый state + confidential-обмен кода с client_secret по TLS + backchannel userinfo (research §17); PKCE конфигурируется per-provider (`pkce`, default true), при `pkce: false` client secret MUST по-прежнему требоваться fail-fast.
- **FR-016**: Внешний access-токен oauth2-провайдера MUST потребляться на месте (получение профиля) и не сохраняться — расширение Assumptions «no external token storage» на оба протокола; метрики/таймауты userinfo-лега наследуют бюджеты SC-005 (`kind=userinfo`, research §19).
- **FR-017**: Экраны `/login` и `/register` MUST визуально соответствовать дизайн-прототипу; нормативный инвентарь (палитра, шрифты, размеры, декоративные элементы, состояние полей/кнопок) — research §22 (для регистрации — §22.6); расхождения с прототипом фиксируются как решения в research, а не в коде.
- **FR-018**: Редизайн MUST быть строго презентационным: парольный вход, перечень провайдеров, authorize-флоу, `returnTo`, отображение ошибок (`role=alert`) и welcome (`role=status`) сохраняют текущее поведение; публичный API-контракт не меняется (дрейф `schema.d.ts` пуст).
- **FR-019**: Тексты интерфейса экранов входа и регистрации MUST быть на русском языке в формулировках прототипа/research §22.6 («ВОЙТИ», «ЗАРЕГИСТРИРОВАТЬСЯ», «Продолжить с …», «Нет аккаунта?», «Уже есть аккаунт?»); доступность MUST сохраняться: видимые placeholder + sr-only `<label>`, `autoComplete`, `aria-label` SSO-секции, переключаемый `aria-label` кнопки показа пароля (US7-3); машинные коды ошибок `problem+json` отображаются существующим механизмом `problemMessage` без перевода.
- **FR-020**: Шрифты Cinzel (600/700/800) и EB Garamond (400, кириллица) MUST подключаться self-host (`@fontsource/*`); внешние шрифтовые CDN и `@import` в рантайме запрещены. Фон — статический ассет `frontend/public/bg2.png`.
- **FR-021**: Новые стили MUST быть изолированы в `frontend/src/auth/pages/auth-theme.css` — общий файл для `LoginPage` и `RegisterPage` (импорт в компонентах, scoped-классы `.auth-*`); глобальный `src/index.css` и стили остальных страниц MUST оставаться без изменений; глобальный `<h1>WebChat</h1>` скрывается только на `/login` и `/register` на уровне разметки `App.tsx` (US7-6/US8-6).
- **FR-022**: `RegisterPage` MUST переиспользовать визуальные компоненты US7 (карточка, бейдж, бренд-блок, поля, кнопка) из общего `auth-theme.css` без дублирования CSS; отличия от login — только состав контента: поля username/email, отсутствие divider «ИЛИ» и SSO-блока, свой заголовок и футер (research §22.6); поведение регистрации (`POST /auth/register`, `problem+json`, success-сообщение о письме) без изменений.

### Key Entities *(include if feature involves data)*

- **Identity Provider (конфигурация провайдера)**: внешний провайдер идентичности; атрибуты: уникальный код, отображаемое имя, endpoints, client id, состояние (включён/выключен), признак доверия (allowlist автосвязывания по email, по умолчанию выключен — opt-in). Секрет (client secret) хранится в secret manager по ссылке, вне БД приложения и репозитория.
- **External Identity (внешняя идентичность)**: привязка внешнего аккаунта к аккаунту WebChat; атрибуты: провайдер, уникальный идентификатор пользователя у провайдера, email от провайдера на момент входа/привязки, момент привязки. Принадлежит ровно одному User; пара (провайдер, идентификатор) уникальна в системе.
- **User (аккаунт, расширение фичи 002)**: может иметь ноль или более внешних идентичностей; JIT-аккаунт создаётся сразу в активном статусе с email от провайдера и сгенерированным username, без пароля; инвариант: у активного аккаунта есть хотя бы один способ входа (пароль или внешняя привязка).
- **Session (сессия, расширение фичи 002)**: единая сущность сессий фичи 002; расширяется признаком способа создания (`password` / `sso`) и необязательной ссылкой на внешнюю идентичность, создавшую сессию (конкретный провайдер определяется через привязку), для журнала событий безопасности (FR-011) и метрик; все свойства (ротация refresh, отзыв, параллельные сессии) наследуются без изменений.

## Success Criteria *(mandatory)*

### Measurable Outcomes

- **SC-001**: Пользователь с привязанной внешней идентичностью проходит вход через провайдера за не более чем 3 взаимодействия с приложением (выбор провайдера — аутентификация у провайдера — возврат в чат; проверяется вручную — quickstart S1); 95% первых входов завершаются успешно без обращения в поддержку *(post-launch метрика)*.
- **SC-002**: 100% операций API чата, доступных парольной сессии, доступны и равноправны для сессии, созданной через провайдера (проверяется автотестами по всему перечню endpoints); продление, ротация refresh и logout работают идентично.
- **SC-003**: Не менее 90% новых пользователей, впервые входящих через провайдера с подтверждённым email, попадают в чат автоматически (JIT или автосвязывание) без единого ручного шага регистрации *(post-launch метрика; автоматизация пути — IT-тесты US2 в tasks.md)*.
- **SC-004**: Аудитом подтверждается: ни один client secret или иной секрет провайдера не обнаруживается в коде, репозитории, конфигурационных файлах и логах; 100% production-секретов хранятся в secret manager (исключение — документированный dev-only dummy-секрет dex в `deploy/local/dex/`, Assumptions; проверяется с явным gitleaks-allowlist для этого каталога).
- **SC-005**: При недоступности любого провайдера 100% попыток входа через него завершаются понятной ошибкой не более чем за 5 секунд (без «вечного ожидания» и системных ошибок), парольный вход и остальные провайдеры не деградируют, 100% сбоев фиксируются в observability.
- **SC-006**: Подделанные или повторно воспроизведённые ответы провайдера отклоняются в 100% случаев без создания сессий, привязок или аккаунтов (проверяется автотестами на подмену и повтор).
- **SC-007**: SSO-маршруты выдерживают smoke-профиль нагрузки (k6, `load/k6/auth.smoke.js`): автоматизированы сценарии providers/authorize/token-error — полный браузерный флоу (callback) в smoke невозможен без реального IdP (ограничение задокументировано в скрипте) и покрывается интеграционными тестами фаз US4/US5; превышения лимитов rate limiting отклоняются корректно и консистентно при горизонтальном масштабировании, а не приводят к отказу сервиса.
- **SC-008**: После редизайна все обновлённые frontend-тесты US7/US8 зелёные (`pnpm --dir frontend test`, вкл. Register-ветку `RegisterPages.test.tsx`), тесты остальных страниц — без правок и регрессий, drift-check контракта пуст; ручная проверка quickstart S9 (оба экрана) пройдена.

## Assumptions

- Поддерживаются два протокольных режима per-provider: OIDC (authorization code flow + PKCE) и `oauth2-userinfo` (research §16, FR-014; ревизия 2026-09-19 — исходное «только OIDC, YAGNI» снято: целевые провайдеры Яндекс/GitHub — OAuth2-only); прочие протоколы (SAML) — вне scope. Расширение на GitHub (email через отдельный эндпоинт `/user/emails`) — задел на будущее, в этой итерации не реализуется.
- Автосвязывание/JIT выполняются только при подтверждённом провайдером email (`email_verified`); автосвязывание с существующими аккаунтами — дополнительно по opt-in allowlist: доверие включается явной конфигурацией провайдера, по умолчанию выключено для всех (защита от захвата аккаунта через «удобный» провайдер, утверждающий чужой email).
- Username JIT-аккаунта генерируется системой из email с гарантией уникальности; смена username и профиля — вне scope этой фичи.
- JIT-аккаунт не имеет пароля до явной установки через восстановление пароля (фича 002); пароль при JIT-создании не запрашивается, письмо с подтверждением email не отправляется (email уже подтверждён провайдером).
- Токены провайдера (access/refresh внешнего IdP) не сохраняются после завершения флоу — системе нужна только идентичность (YAGNI).
- Конфигурирование провайдеров — операционная процедура (конфигурация + secret manager), без UI администратора в этой фиче; включение/выключение отражается на экране входа без изменения клиентов.
- Единственное исключение из SC-004/FR-007 — локальная разработка: конфиг dex в `deploy/local/dex/` содержит staticClient с заведомо неиспользуемым в production dummy-секретом (`dev-secret`), передаваемым backend'у через env `SSO_DEX_CLIENT_SECRET`. Критерии «не секрет» (все обязательны): (1) значение документировано и заведомо публично; (2) не используется ни в одном окружении, кроме локального docker-compose; (3) путь покрыт узким gitleaks allowlist `deploy/local/dex/.*`. Literal-секреты вне `deploy/local/dex/` запрещены без исключений.
- Единый logout / выход из провайдера (front-/back-channel logout), обмен утверждениями о пользователе между провайдерами — вне scope: выход из WebChat отзывает только сессию WebChat.
- Привязка провайдера с email, отличным от email аккаунта, разрешена (пользователь подтверждает контроль обеих сторон); email аккаунта при этом не меняется и на сопоставление JIT не влияет.
- Открытие экрана входа со списком провайдеров и старт SSO-флоу доступны без аутентификации; полный перечень публичных endpoints обновляется в API-контракте (расширение перечня из фичи 002).
- Нормализация email (регистронезависимость, обрезка пробелов) соответствует фиче 002; сопоставление при JIT/автосвязывании выполняется по нормализованному значению.
- Модель сессий/токенов фичи 002 не изменяется — только расширяется признаком способа входа; обратная совместимость публичного API обязательна (конституция IV).
- Встраиваемый виджет (фича 013) использует те же SSO-флоу через то же публичное API; отдельные механизмы для него не создаются.
- Блокировка администратором отдельных провайдеров для отдельных пользователей/групп, условный доступ, MFA-политики — вне scope (могут быть отдельными фичами).
- «Понятная ошибка» в SSO-сценариях = конкретный публичный код из таблицы `sso_error` (contracts/sso-api.md §3) + пользовательский текст без внутренних деталей; перечень кодов в spec не дублируется.
- Дизайн-прототип экрана входа (`tmp/login.html` + `tmp/bg2.png`) — нормативный источник визуала US7; каталог `tmp/` не версионируется (`.gitignore`), инвентарь и отклонения зафиксированы в research §22. Для US8 (регистрация) прототипа нет — экран строится на инвентаре US7 с составом полей регистрации (research §22.6). Кнопка «Microsoft» из прототипа не входит в US7: состав SSO-кнопок определяется только `GET /auth/sso/providers`.
- Редизайн остальных auth-страниц (confirm-registration, set-password, forgot/reset-password, sso/callback) и внедрение темы «прототипа» за пределами `/login` и `/register` — вне scope US7/US8 (YAGNI до отдельной итерации).
- Тема экранов входа и регистрации фиксированно тёмная (палитра прототипа), не следует системному `prefers-color-scheme`; остальные страницы сохраняют текущую двойную схему.

## Проверка согласованности с конституцией

- Принцип V (SSO/OIDC как равноправный способ входа; секреты только в secret manager) — FR-001, FR-007, SC-004.
- Принцип II (stateless, консистентные лимиты при масштабировании) — FR-009, SC-007.
- Принцип IV (публичный API, обратная совместимость; расширение перечня публичных endpoints через контракт) — FR-008.
- Принцип VI (интеграционные тесты обязательны для SSO) — Independent Tests всех историй, SC-002, SC-006.
- Принцип VII (YAGNI: без admin-UI, без хранения внешних токенов, без single logout) — Assumptions.
- US7/US8 (расширение 2026-09-20): принцип IV — контракт 0.3.0 не меняется, drift-check пуст (FR-018, SC-008); принцип VI — обновление frontend-тестов первыми, RED→GREEN (Phase 10–11 tasks.md); принцип VII — scope ограничен `/login` и `/register`, без общей темизации (Assumptions).
