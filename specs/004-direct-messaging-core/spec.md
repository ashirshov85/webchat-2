# Feature Specification: Личный текстовый диалог в реальном времени с exactly-once доставкой

**Feature Branch**: `004-direct-messaging-core`

**Created**: 2026-09-20

**Status**: Draft

**Input**: User description: "Два пользователя ведут личный диалог текстовыми сообщениями в реальном времени. Сообщения доставляются гарантированно и без дублей (эффект exactly-once для пользователя): клиент генерирует уникальный ID сообщения, сервер дедуплицирует по ID, подтверждает запись клиенту, порядок сообщений сохраняется в рамках чата, повторная отправка (ретрай) безопасна и не создаёт дубликат. История переписки доступна при повторном входе. Подключение к реалтайм-каналу через публичный API (OpenAPI-контракт — источник истины). Состояние сообщений: отправляется / доставлено. Дополнение: пользователю в элементе «сообщения» показываются два статуса — доставлено и прочитано; доставлено визуализируется одной галочкой, прочитано — двойной галочкой."

## Clarifications

### Session 2026-09-20

- Q: Как пользователь начинает новый личный диалог? → A: Через панель контактов слева: поиск пользователя по email или логину → добавление в свои контакты → нажатие на контакт начинает или продолжает единый диалог пары.
- Q: Что показывает левая панель помимо контактов? → A: Панель разделена на режимы «Чаты» и «Контакты». В режиме «Контакты» находится окно поиска контактов и список добавленных контактов, упорядоченный по email или логину. В режиме «Чаты» находится окно поиска существующих чатов и один объединённый список — чаты (диалоги) и (в будущем) групповые чаты сортируются вместе по дате последнего полученного сообщения; непрочитанные показываются индикатором, но на порядок не влияют; элементы без переписки располагаются ниже.
- Q: Что происходит у получателя, когда ему пишет пользователь, которого нет в его контактах? → A: Диалог автоматически появляется в панели получателя при первом входящем сообщении; для чтения и ответа не нужно добавлять отправителя в контакты.
- Q: Что происходит с сообщением «отправляется» при закрытии приложения до подтверждения сервера? → A: Сообщение сохраняется на клиенте и автоматически ретраится тем же ID при следующем запуске/входе.
- Q: Какое максимальное количество символов допустимо в одном текстовом сообщении? → A: 4096 символов.
- Q: Нужны ли в этой фиче, помимо добавления контакта, также удаление контакта и блокировка пользователя? → A: Да — добавление, удаление контакта и блокировка пользователей входят в объём фичи.
- Q: Как блокировка пользователя должна отражаться на диалоге и отправке сообщений? → A: Отправка запрещена в обе стороны с понятной ошибкой; диалог у блокирующего видим с пометкой «заблокирован»; история сохраняется; разблокировка возвращает обычное поведение.
- Q: Какой лимит частоты отправки сообщений зафиксировать для защиты от флуда? → A: 30 сообщений в минуту на пользователя.
- Q: Как должен срабатывать поиск пользователей по email или логину? → A: Точное совпадение по полному email или полному логину, без учёта регистра.
- Q: Как должен выглядеть индикатор непрочитанных сообщений в панели слева? → A: Числовой счётчик непрочитанных, при большом числе — «99+».
- Q: Как разделить панель слева — контакты и чаты? → A: Панель переключается кнопкой между режимами: «Чаты» — личные чаты и (в будущем) групповые чаты одним списком, сортировка по дате последнего сообщения; «Контакты» — контакты, сортировка по логину или email. Контакты и чаты/групповые чаты удаляются отдельно; удаление чата удаляет все сообщения из истории этого пользователя.
- Q: Как именно сортируются контакты в режиме «Контакты»? → A: Переключаемая алфавитная сортировка: по логину или по email, поле выбирает пользователь.
- Q: Что происходит при удалении чата? → A: Удаление только у удалившего: собеседник сохраняет чат и историю; при новом входящем сообщении чат снова появляется у удалившего, удалённая история не восстанавливается.
- Q: Сколько сообщений должна возвращать одна страница истории при подгрузке более старых сообщений? → A: 50 сообщений на страницу.
- Q: Что видит заблокированный пользователь при попытке отправить сообщение в диалог с блокирующим? → A: Явная ошибка: пользователь прямо уведомлён, что он заблокирован.
- Q: Должно ли сообщение из одних пробелов считаться пустым и отклоняться? → A: Да — отклоняется, если текст пуст после trim; внутренние пробелы сохраняются.
- Q: Разрешить ли пользователю добавлять себя в контакты и вести диалог с самим собой? → A: Нет — и добавление себя, и диалог с собой запрещены с понятной ошибкой.
- Q: Как блокировка взаимодействует со списком контактов — остаётся ли заблокированный в контактах у блокирующего и может ли заблокированный добавить блокирующего в свои контакты? → A: Контакты и блокировки независимы: контакт остаётся в списке блокирующего; заблокированный может добавить блокирующего в свои контакты; блокировка проявляется только ошибкой при отправке сообщений.
- Q: Что происходит с локально сохранённым неподтверждённым сообщением («отправляется»), если повторная отправка окончательно отклонена (например, получатель заблокировал отправителя)? → A: Сообщение переходит в терминальный клиентский статус «не отправлено» с показом ошибки; автоматические ретраи прекращаются.
- Q: Что видит заблокированный пользователь в своём списке «Чаты» — остаётся ли там диалог с блокирующим? → A: Диалог остаётся в списке «Чаты» заблокированного без специальной пометки; о блокировке пользователь узнаёт только из явной ошибки при попытке отправки.
- Q: Отмечаются ли сообщения «прочитано» (двойная галочка у отправителя), когда блокирующий просматривает историю диалога с заблокированным? → A: Нет — пока блокировка действует, события прочтения не формируются и не доставляются заблокированному; статусы замирают, монотонность сохраняется; после разблокировки поведение возобновляется.
- Q: Двигается ли водяной знак прочтений заблокированного, пока действует блокировка? → A: Да — локально и молча: событие блокирующему не публикуется (обновление ✓✓ у блокирующего возможно только при повторной загрузке диалога, не в реальном времени); после разблокировки состояние монотонно сходится.
- Q: Как клиент должен обрабатывать отклонение отправки по флуд-лимиту (более 30 сообщений в минуту)? → A: Флуд-лимит — временная ошибка: клиент автоматически повторяет отправку тем же уникальным ID после окончания лимитного окна, с индикацией задержки пользователю.
- Q: Что происходит с локально сохранёнными неподтверждёнными сообщениями («отправляется») удаляемого чата при удалении чата? → A: Сообщения «отправляется» удаляются вместе с чатом; автоматические ретраи прекращаются.
- Q: Как ведёт себя числовой бейдж непрочитанных на диалоге с заблокированным у блокирующего при действующей блокировке? → A: Бейдж «замирает»: сохраняет значение на момент блокировки — не растёт и не сбрасывается до разблокировки.
- Q: Нужно ли включить в спецификацию требования к наблюдаемости (логи и метрики)? → A: Да, минимально: логи отклонённых отправок (флуд, блокировка) и метрики доставки/дедупликации/realtime-латентности (SC-008).
- Q: Должен ли чат менять позицию в списке «Чаты» при отправке исходящего сообщения, или сортировка учитывает только входящие? → A: Любое последнее сообщение диалога — входящее или исходящее — обновляет позицию чата в списке.
- Q: Что пользователь может сделать с сообщением в терминальном статусе «не отправлено»? → A: Ручной повтор тем же уникальным ID и локальное удаление сообщения.
- Q: По какому тексту измеряется лимит 4096 символов — до или после обрезки пробелов? → A: После trim — обе проверки (пустота и длина) применяются к нормализованному тексту.

## User Scenarios & Testing *(mandatory)*

### User Story 1 - Обмен текстовыми сообщениями в личном диалоге в реальном времени (Priority: P1)

Два аутентифицированных пользователя ведут личный диалог (строгая пара «один на один») текстовыми сообщениями. Отправитель пишет текст и отправляет сообщение: оно сразу появляется у отправителя со статусом «отправляется», а после подтверждения сервером факта записи переходит в статус «доставлено». Получатель, находящийся онлайн, видит новое сообщение в чате в реальном времени, без ручного обновления. У пары пользователей всегда один и тот же общий диалог.

**Why this priority**: Это ядро фичи и минимальный жизнеспособный продукт «личный чат»: без отправки, мгновенной доставки и видимых статусов сообщений остальные сценарии не имеют смысла.

**Independent Test**: Полностью проверяется вручную и автотестом: два пользователя открывают общий диалог; один отправляет сообщение; у отправителя статус меняется «отправляется» → «доставлено»; у онлайн-получателя сообщение появляется в реальном времени.

**Acceptance Scenarios**:

1. **Given** два аутентифицированных пользователя и получатель онлайн, **When** отправитель отправляет непустое текстовое сообщение, **Then** оно немедленно отображается у отправителя со статусом «отправляется», и после подтверждения сервером записи статус меняется на «доставлено».
2. **Given** получатель онлайн и подключён к реалтайм-каналу диалога, **When** сообщение записано на сервере, **Then** оно появляется у получателя в реальном времени без ручного обновления.
3. **Given** сервер не подтвердил запись сообщения (сбой, таймаут), **When** отправитель смотрит на чат, **Then** сообщение остаётся видимым со статусом «отправляется» и не считается доставленным.
4. **Given** пустой текст или текст длиннее 4096 символов, **When** отправитель пытается отправить сообщение, **Then** отправка отклоняется с понятной ошибкой и в чате ничего не появляется.
5. **Given** пользователь, не являющийся участником диалога, **When** пытается читать диалог или отправлять в него сообщения, **Then** доступ отклоняется (авторизация на уровне ресурса).

---

### User Story 2 - Гарантированная доставка без дублей с сохранением порядка (Priority: P1)

Отправитель может столкнуться с нестабильной сетью, таймаутами, повторными отправками (ретраями) и двойным нажатием «отправить». Для пользователя результат всегда один: сообщение записано ровно один раз. Клиент генерирует уникальный ID сообщения; сервер дедуплицирует по этому ID и подтверждает запись. Повторная отправка того же сообщения с тем же ID безопасна и не создаёт дубликат — клиент получает подтверждение той же записи. Порядок сообщений сохраняется в рамках чата: оба участника видят одну и ту же последовательность, не меняющуюся при перезагрузках.

**Why this priority**: Гарантия «не теряется, не дублируется, порядок стабилен» — неотъемлемое качество продукта и конституционное требование (принцип III); без неё диалог непригоден для общения.

**Independent Test**: Проверяется автоматизированными тестами доставки: (а) повторная отправка с тем же ID (симуляция ретрая и двойного клика) не создаёт второй экземпляр; (б) в чате и в истории ровно один экземпляр каждого сообщения; (в) последовательность сообщений одинакова у обоих участников и между загрузками.

**Acceptance Scenarios**:

1. **Given** отправленное сообщение, для которого клиент не получил подтверждение (таймаут, обрыв связи), **When** клиент повторяет отправку с тем же уникальным ID, **Then** в чате остаётся ровно один экземпляр сообщения, а клиент получает подтверждение той же записи.
2. **Given** отправитель дважды инициирует отправку одного сообщения, **When** обе попытки уходят с одним и тем же ID, **Then** получатель видит одно сообщение, а не два.
3. **Given** несколько сообщений, отправленных подряд одним или обоими участниками, **When** они доставляются и загружаются из истории, **Then** порядок внутри чата одинаков для обоих участников и не меняется при повторных загрузках.
4. **Given** получатель был офлайн в момент отправки, **When** он подключается и открывает диалог, **Then** все записанные сообщения доступны ему — ничего не потеряно и нет дублей.
5. **Given** соединение участника с реалтайм-каналом прервалось и восстановилось, **When** канал снова готов, **Then** состояние чата сходится к серверному: без потерь, дублей и изменения порядка.
6. **Given** один пользователь работает с нескольких устройств (параллельные сессии), **When** в диалоге появляются новые сообщения, **Then** состояние чата сходится во всех сессиях пользователя без дублей и потерь.
7. **Given** приложение отправителя закрыто (сессия завершена) до подтверждения записи сообщения, **When** пользователь снова входит, **Then** неподтверждённое сообщение автоматически отправляется повторно с тем же уникальным ID, и в результате в чате остаётся ровно один экземпляр сообщения.

---

### User Story 3 - История переписки доступна при повторном входе (Priority: P2)

Пользователь закрывает приложение или завершает сессию и позже входит снова. Вся переписка личного диалога доступна: при открытии чата пользователь видит последние сообщения в правильном порядке, а длинная история подгружается по частям при прокрутке — вплоть до начала переписки.

**Why this priority**: История делает диалог инструментом длительного общения, а не одноразовым сеансом; при этом реалтайм-обмен уже ценен и без неё, поэтому приоритет ниже первых двух историй.

**Independent Test**: Проверяется вручную и автотестом: участники обмениваются сообщениями; один выходит и входит снова; при открытии диалога видны последние сообщения в исходном порядке; прокрутка подгружает более старые части до начала переписки.

**Acceptance Scenarios**:

1. **Given** диалог содержит много сообщений, **When** участник повторно входит и открывает диалог, **Then** он видит последние сообщения и может по частям загрузить более старые — до начала переписки.
2. **Given** участник повторно вошёл и читает историю, **When** сообщения отображаются, **Then** их порядок совпадает с тем, что был виден ранее, и каждое записанное сообщение имеет статус «доставлено».
3. **Given** диалог пуст, **When** участник открывает его, **Then** отображается корректное пустое состояние без ошибок.
4. **Given** запрос более старой части истории выходит за начало переписки, **When** клиент запрашивает следующую часть, **Then** возвращается пустой результат без ошибок (корректная граница пагинации).

---

### User Story 4 - Статус «прочитано»: отправитель видит, что сообщение просмотрено (Priority: P2)

Отправитель видит свои исходящие сообщения в диалоге с индикаторами статуса: «доставлено» — одна галочка, «прочитано» — двойная галочка. Сообщение становится «прочитано», когда получатель просматривает его: сообщение отображается получателю в открытом диалоге (включая чтение истории при повторном входе). Если получатель онлайн, галочки у отправителя обновляются в реальном времени, без перезагрузки. Индикаторы статуса показываются только на исходящих сообщениях пользователя; входящие сообщения получателя отображаются без галочек.

**Why this priority**: Индикатор «прочитано» завершает цикл общения — отправитель знает, что сообщение не только записано сервером, но и увидено получателем; опирается на уже работающую доставку и просмотр, поэтому приоритет P2.

**Independent Test**: Проверяется вручную и автотестом: отправитель отправляет сообщение — после подтверждения записи одна галочка; получатель открывает диалог и просматривает сообщение — у отправителя двойная галочка без перезагрузки; пока получатель не открывал диалог, остаётся одна галочка.

**Acceptance Scenarios**:

1. **Given** записанное сообщение отображается получателю в открытом диалоге, **When** получатель его просматривает, **Then** у отправителя статус меняется с «доставлено» (одна галочка) на «прочитано» (двойная галочка).
2. **Given** отправитель и получатель онлайн, **When** получатель просматривает сообщение, **Then** статус у отправителя обновляется в реальном времени, без перезагрузки.
3. **Given** получатель офлайн либо не открывал диалог после отправки, **When** отправитель смотрит на чат, **Then** у сообщения остаётся одна галочка «доставлено».
4. **Given** получатель повторно входит и читает историю диалога, **When** сообщения отображаются ему, **Then** они отмечаются прочитанными, и отправитель видит двойные галочки.
5. **Given** сообщение уже в статусе «прочитано», **When** происходят повторные открытия диалога, переподключения или дубли событий прочтения, **Then** статус не откатывается к «доставлено» и не меняется некорректно (статус монотонен, события идемпотентны).
6. **Given** диалог получателя открыт в момент прихода нового сообщения, **When** сообщение отображается получателю, **Then** оно сразу отмечается прочитанным после отображения.

---

### User Story 5 - Управление контактами и чатами на панели слева (Priority: P2)

Панель слева имеет два режима, переключаемых кнопкой: «Чаты» (по умолчанию) и «Контакты». В режиме «Чаты» личные чаты пользователя (и в будущем групповые чаты) отображаются одним списком, упорядоченным по дате последнего сообщения, с числовым бейджем непрочитанных. В режиме «Контакты» отображается список контактов с алфавитной сортировкой по логину или email — поле выбирает пользователь. Новый диалог инициируется через режим «Контакты»: поиск пользователя по полному email или логину, добавление в контакты, нажатие на контакт открывает единый диалог пары. Контакты и чаты — независимые сущности: удаление контакта не удаляет чат, удаление чата удаляет все сообщения только из истории удалившего пользователя.

**Why this priority**: Панель — основной вход в переписку и контекст всех пользовательских сценариев, но сам обмен сообщениями технически от неё не зависит, поэтому приоритет P2, а не P1.

**Independent Test**: Проверяется вручную и автотестом: контакт добавляется через поиск и клик по нему открывает единый диалог; удаление контакта сохраняет чат и историю; удаление чата скрывает историю только у удалившего; входящее сообщение от незнакомца автоматически создаёт чат в списке получателя.

**Acceptance Scenarios**:

1. **Given** у пользователя есть переписки, **When** он входит и открывает панель слева (режим «Чаты»), **Then** чаты упорядочены по дате последнего сообщения диалога — как входящего, так и исходящего (чаты без сообщений — ниже), непрочитанные отображаются числовым бейджем (при большом числе — «99+»), и он может открыть переписку в любом из них.
2. **Given** аутентифицированный пользователь, **When** он ищет пользователя по полному email или логину и добавляет его в свои контакты, **Then** контакт отображается в режиме «Контакты», и нажатие на него открывает единый диалог пары (создавая его при первом обращении); чат отображается в режиме «Чаты».
3. **Given** панель слева, **When** пользователь переключает кнопку режимов, **Then** режим меняется между «Чаты» и «Контакты», контакты отсортированы алфавитно по выбранному полю (логин или email), состояние списков сохраняется.
4. **Given** отправитель написал пользователю, у которого нет отправителя в контактах, **When** сообщение записано на сервере, **Then** чат с отправителем автоматически появляется в списке «Чаты» получателя, и получатель может читать сообщение и отвечать без предварительного добавления контакта.
5. **Given** пользователь удаляет контакт, с которым есть переписка, **When** удаление выполнено, **Then** чат и история пары сохраняются, обмен сообщениями продолжается.
6. **Given** пользователь удаляет чат из своего списка «Чаты», **When** удаление выполнено, **Then** чат и все его сообщения исчезают из истории этого пользователя; у собеседника чат и история сохраняются; при новом входящем сообщении от собеседника чат снова появляется у удалившего, без ранее удалённой истории.

---

### User Story 6 - Подключение к реалтайм-каналу через публичный API-контракт (Priority: P3)

Реалтайм-канал (события новых сообщений диалога) — часть публичного API и описан в OpenAPI-контракте, который является источником истины. Клиенты проекта (standalone SPA, встраиваемый виджет) и любой сторонний клиент, следующий контракту, подключаются к каналу и работают с сообщениями через одни и те же публичные интерфейсы: отправка подчиняется тем же правилам уникальности ID и дедупликации, события канала соответствуют контрактным схемам.

**Why this priority**: Принцип API-First и встраиваемость — стратегическая цель проекта, но уже один клиент, построенный строго по контракту, проверяет его на практике, поэтому приоритет ниже пользовательских сценариев обмена.

**Independent Test**: Проверяется автотестом контракта: реалтайм-канал и операции сообщений описаны в OpenAPI-контракте; клиент, построенный строго по контракту (без внутренних интерфейсов), успешно подключается, отправляет и получает сообщения.

**Acceptance Scenarios**:

1. **Given** клиент, построенный строго по OpenAPI-контракту, подключён к реалтайм-каналу диалога участника, **When** в диалоге записывается новое сообщение, **Then** клиент получает событие нового сообщения, соответствующее контрактной схеме.
2. **Given** клиент, построенный строго по контракту, отправляет сообщение с уникальным ID через контрактную операцию, **When** сервер подтверждает запись, **Then** подтверждение соответствует контракту, а повторная отправка с тем же ID не создаёт дубликат.
3. **Given** контракт публичного API обновляется, **When** выпускается новая версия, **Then** обратная совместимость публичных интерфейсов сообщений и реалтайм-канала сохраняется (breaking changes — только через версионирование по конституции).

### Edge Cases

Сценарии, не покрытые acceptance-сценариями пользовательских историй; остальное дедуплицировано в FR.

- Текст ровно 4096 символов (после trim) проходит; 4097+ и пустой — отклоняются (граница валидации).
- Текст только из пробелов/переводов строк — отклоняется как пустой (валидация по trim; FR-003).
- Попытка открыть диалог с несуществующим пользователем — понятная ошибка.
- Добавление себя в контакты и открытие диалога с самим собой — отклоняются с понятной ошибкой (строгая пара «один на один», FR-001).
- Поиск пользователя по email/логину без совпадений — корректный пустой результат без ошибок.
- Повторное добавление уже имеющегося контакта — дубликат в списке контактов не создаётся.
- Блокировка при открытом диалоге или активной реалтайм-подписке — состояние обоих участников сходится к заблокированному, неявная доставка исключена.
- Повторная блокировка уже заблокированного и разблокировка не заблокированного пользователя — идемпотентны, ошибок и дублей состояния не создают.
- Интенсивная отправка сообщений (флуд) — более 30 сообщений в минуту от пользователя отклоняется с понятной ошибкой, записей не создаётся.
- Открытие диалога с большим количеством непрочитанных сообщений — все отображённые сообщения корректно отмечаются прочитанными (включая подгруженные части истории).
- Удаление чата, у которого есть локально сохранённые сообщения «отправляется», — эти сообщения удаляются вместе с чатом, автоматические ретраи прекращаются (FR-021).
- Диалог с заблокированным пользователем у блокирующего — бейдж непрочитанных «замирает»: не растёт и не сбрасывается, пока действует блокировка (FR-020).

## Requirements *(mandatory)*

### Functional Requirements

Диалог и доступ:

- **FR-001**: Система MUST предоставлять аутентифицированным пользователям личный диалог «один на один»; для пары пользователей существует ровно один диалог, и оба участника имеют равный доступ на чтение и отправку.
- **FR-002**: Доступ к диалогу (чтение, отправка, удаление чата, реалтайм-подписка) MUST быть разрешён только участникам этого диалога; всем остальным — отказ на уровне каждого ресурса.

Сообщения:

- **FR-003**: Система MUST валидировать текст сообщения по единой нормализации: текст обрезается по начальным и конечным пробелам/переводам строк (внутренние пробелы сохраняются), затем нормализованный текст MUST быть непустым и не длиннее 4096 символов (значение фиксируется в API-контракте); невалидная отправка отклоняется с понятной ошибкой.
- **FR-004**: Клиент MUST генерировать уникальный ID каждого сообщения; система MUST дедуплицировать записи по этому ID: повторная запись того же ID не создаёт новый экземпляр, а возвращает подтверждение уже существующей записи.
- **FR-005**: Система MUST подтверждать клиенту факт записи сообщения; до подтверждения сообщение отображается отправителю со статусом «отправляется», после подтверждения — «доставлено», после просмотра получателем — «прочитано». Статусы «доставлено» и «прочитано» визуализируются в элементе сообщения: одна галочка и двойная галочка соответственно; индикаторы отображаются только на исходящих сообщениях пользователя; смена статусов монотонна (не откатывается назад).
- **FR-006**: Система MUST сохранять порядок сообщений в рамках одного чата: последовательность одинакова для обоих участников и стабильна при повторных загрузках.
- **FR-007**: Система MUST доставлять новые сообщения онлайн-получателю через реалтайм-канал без ручного обновления; сообщение для офлайн-получателя MUST быть доступно из истории при подключении.
- **FR-008**: Система MUST сохранять историю переписки диалога и предоставлять её при повторном входе участника с постраничной загрузкой более старых сообщений; размер страницы — 50 сообщений (значение фиксируется в API-контракте).
- **FR-009**: При переподключении клиента (обрыв связи, повторный вход, новая сессия) состояние чата MUST сходиться к серверному: без потерь, дублей и изменения порядка.
- **FR-010**: Система MUST отмечать сообщение «прочитано», когда оно отображается получателю в открытом диалоге, включая просмотр истории при повторном входе; изменение статуса MUST доставляться отправителю в реальном времени. События прочтения идемпотентны, статус монотонен — повторная обработка не откатывает и не искажает статус.
- **FR-011**: Система MUST ограничивать частоту отправки сообщений (защита от флуда): не более 30 сообщений в минуту на пользователя (значение фиксируется в API-контракте); превышение отклоняется с понятной ошибкой и не создаёт записей.
- **FR-012**: Клиент MUST локально сохранять неподтверждённые сообщения (статус «отправляется») и автоматически повторять их отправку с тем же уникальным ID при следующем запуске приложения или повторном входе; результат сходится к одному экземпляру сообщения. Временные отклонения — обрыв связи, таймаут, флуд-лимит (FR-011) — клиент MUST автоматически повторять отправку с тем же уникальным ID; при флуд-лимите повтор выполняется после окончания лимитного окна с индикацией задержки пользователю. Если повторная отправка окончательно отклонена сервером (например, получатель заблокировал отправителя), клиент MUST переводить сообщение в терминальный статус «не отправлено» с показом понятной ошибки и прекращать автоматические ретраи. Для сообщения в статусе «не отправлено» пользователь MUST иметь возможность вручную повторить отправку с тем же уникальным ID (результат сходится к одному экземпляру благодаря дедупликации FR-004) либо локально удалить это сообщение из своего чата.

Панель слева:

- **FR-013**: Панель слева MUST иметь два режима, переключаемых кнопкой: «Чаты» (по умолчанию) и «Контакты»; состояние списков при переключении сохраняется.
- **FR-014**: В режиме «Чаты» личные чаты пользователя (и в будущих фичах групповые чаты) MUST отображаться одним списком, упорядоченным по дате последнего сообщения диалога — как входящего, так и исходящего (отправка исходящего сообщения также поднимает чат в списке); чаты без сообщений располагаются после чатов с сообщениями; непрочитанные сообщения отображаются числовым бейджем-счётчиком (при большом числе — «99+»), который не влияет на порядок и сбрасывается по мере прочтения сообщений (FR-010).
- **FR-015**: В режиме «Контакты» контакты пользователя MUST отображаться с алфавитной сортировкой по логину или по email — поле сортировки выбирает пользователь.

Контакты:

- **FR-016**: Система MUST предоставлять пользователю поиск других пользователей по точному совпадению полного email или полного логина (без учёта регистра) и добавление найденного пользователя в его личный список контактов; добавление самого себя MUST отклоняться с понятной ошибкой; контакты отображаются в режиме «Контакты» панели слева.
- **FR-017**: Система MUST позволять удалять контакт из списка; удаление контакта — независимая операция: единый диалог пары и его история сохраняются, переписка остаётся доступной и продолжается.
- **FR-018**: Нажатие на контакт в режиме «Контакты» MUST открывать единый диалог пары пользователей, создавая диалог при первом обращении; повторные обращения всегда ведут к одному и тому же диалогу; открывшийся чат отображается в режиме «Чаты».
- **FR-019**: Чат MUST автоматически появляться в списке «Чаты» получателя при первом входящем сообщении от пользователя, отсутствующего в его контактах; чтение и ответ не требуют предварительного добавления отправителя в контакты.

Блокировка и удаление чата:

- **FR-020**: Система MUST предоставлять пользователю возможность блокировать и разблокировать другого пользователя. Блокировка односторонняя (не требует действия второй стороны). Пока блокировка действует: отправка сообщений в диалоге пары MUST быть запрещена в обе стороны и отклоняться с понятной ошибкой без создания записей; заблокированный пользователь при попытке отправки MUST получать явную ошибку, прямо уведомляющую, что он заблокирован; единый диалог остаётся видим у блокирующего с пометкой «заблокирован»; у заблокированного диалог также остаётся в списке «Чаты» без пометки — о блокировке он узнаёт только из явной ошибки при отправке; история переписки сохраняется; просмотр истории блокирующим MUST NOT отмечать сообщения «прочитано» и MUST NOT доставлять заблокированному события прочтения; прочтения заблокированного в период блокировки двигают его водяной знак локально, но событие прочтения блокирующему не публикуется — блокирующий видит эти «прочитано» (✓✓) только при повторной загрузке диалога, не в реальном времени; в realtime-канале статусы замирают, состояние монотонно и после разблокировки сходится; числовой бейдж непрочитанных (FR-014) этого диалога у блокирующего «замирает»: сохраняет значение на момент блокировки — не растёт и не сбрасывается до разблокировки. Блокировка не затрагивает списки контактов: контакт блокирующего остаётся в его списке «Контакты»; заблокированный может добавлять блокирующего в свои контакты — ограничение проявляется только при отправке сообщений. Разблокировка MUST возвращать обычное поведение диалога, включая события прочтения (FR-010), без потери истории.
- **FR-021**: Пользователь MUST иметь возможность удалить чат из своего списка «Чаты» — операция, независимая от удаления контакта. Удаление чата MUST удалять все сообщения этого чата из истории удалившего пользователя; локально сохранённые неподтверждённые сообщения (статус «отправляется») этого чата MUST удаляться вместе с чатом, автоматические ретраи для них прекращаются. У второго участника чат и история MUST сохраняться. При новом входящем сообщении от собеседника чат MUST снова автоматически появиться в списке «Чаты» удалившего пользователя; ранее удалённая история при этом не восстанавливается.

Публичный API:

- **FR-022**: Реалтайм-канал и операции над сообщениями, диалогами, контактами и блокировками MUST быть описаны в публичном OpenAPI-контракте — источнике истины; изменения публичных интерфейсов MUST сохранять обратную совместимость.

### Key Entities

- **Диалог (личный чат)**: беседа ровно двух пользователей; для пары участников существует единственный диалог; содержит упорядоченную историю сообщений; доступен только участникам; отображается в списке «Чаты» каждого участника. Удаление чата пользователем — per-user операция: удаляет сообщения только из истории удалившего, не затрагивая историю второго участника.
- **Сообщение**: единица переписки; ключевые атрибуты: уникальный ID (генерируется клиентом), диалог, отправитель, текст, момент записи сервером, позиция в порядке чата, факт и момент прочтения получателем; пользовательские статусы: «отправляется» (до подтверждения записи), «доставлено» (после подтверждения, одна галочка), «прочитано» (после просмотра получателем, двойная галочка); терминальный клиентский статус «не отправлено» (после окончательного отклонения, например блокировки; автоповторы прекращаются, доступны ручной повтор с тем же ID и локальное удаление).
- **Участник**: аутентифицированный пользователь, связанный с диалогом; пользователи и аутентификация существуют в системе с фичи регистрации (002).
- **Контакт**: односторонняя запись в личном списке контактов пользователя, созданная через поиск по email или логину; отображается в режиме «Контакты» панели слева; нажатие на контакт ведёт к единому диалогу пары (пользователь ↔ контакт).
- **Блокировка**: одностороннее отношение между двумя пользователями, управляемое блокирующим (включение и снятие). Пока действует, запрещает отправку сообщений в диалоге пары в обе стороны; не удаляет единый диалог и его историю; отображается пометкой «заблокирован» только у блокирующего — диалог остаётся в списке «Чаты» обоих участников. Не затрагивает списки контактов участников: контакт блокирующего сохраняется, заблокированный может добавить блокирующего в контакты; единственное воздействие — запрет отправки сообщений.

## Success Criteria *(mandatory)*

### Measurable Outcomes

- **SC-001**: 95% сообщений при нормальной сети получают статус «доставлено» не позднее 2 секунд после нажатия отправки.
- **SC-002**: В сценариях ретраев, двойных отправок и переподключений пользователи наблюдают 0 дублей — каждое сообщение отображается ровно одним экземпляром (подтверждается автоматизированными тестами доставки).
- **SC-003**: Порядок сообщений в рамках чата одинаков у обоих участников и между повторными загрузками в 100% прогонов автоматизированных тестов.
- **SC-004**: В 95% случаев при повторном входе последние сообщения диалога видимы не позднее 3 секунд после открытия чата, а загрузка каждой более старой части истории занимает не более 3 секунд.
- **SC-005**: Онлайн-получатель видит новое сообщение не позднее 2 секунд с момента подтверждения записи отправителю в 95% случаев.
- **SC-006**: Решение укладывается в бюджеты масштабируемости конституции (одновременные пользователи, сообщения в секунду, stateless-сервисы) — подтверждается расчётом в plan.md и smoke-нагрузочными проверками (k6: целевые p95-латентности, отсутствие 5xx при 429); полномасштабная валидация пиковых бюджетов (1M CCU / 100k msg/s) — платформенная cross-cutting-задача вне объёма фичи (ROADMAP).
- **SC-007**: У онлайн-отправителя статус сообщения меняется на «прочитано» (двойная галочка) не позднее 2 секунд с момента отображения сообщения получателю в 95% случаев.
- **SC-008**: Система предоставляет минимальные сигналы наблюдаемости: журналирование отклонённых отправок (флуд-лимит FR-011, блокировка FR-020) и метрики — латентность доставки (до подтверждения записи и до реалтайм-получателя), доля повторных (дедуплицированных) записей по ID сообщения, частота отклонений по лимитам; по этим сигналам можно проверить выполнение SC-001–SC-007 в эксплуатации.

## Assumptions

- Аутентификация, сессии и зарегистрированные пользователи существуют (фича 002); данная фича не решает вход и регистрацию.
- Поддерживаются только текстовые сообщения; другие типы контента (изображения, видео, виджеты) — отдельные фичи через расширяемую систему типов контента.
- Диалог строго двух участников; групповые чаты — фича 006 (список «Чаты» панели рассчитан на их добавление в будущем).
- Индикатор набора текста, редактирование и удаление отдельных сообщений — вне рамок этой фичи (удаление чата целиком — в рамках, см. FR-021).
- E2EE-беседы вне рамок этой фичи и описываются отдельно.
- Реалтайм-доставка означает доставку онлайн-клиенту в приложении/виджете; push-уведомления вне приложения (web push, email) — вне рамок.
- Количественные параметры (лимит 4096 символов, лимит 30 сообщений/мин, размер страницы истории) зафиксированы в соответствующих FR и API-контракте; здесь не дублируются.
- Полномасштабные нагрузочные тесты пиковых бюджетов конституции (1M CCU / 100k msg/s) — платформенная cross-cutting-задача вне объёма фичи; в рамках фичи — расчёт в plan.md и k6 smoke (SC-006).
