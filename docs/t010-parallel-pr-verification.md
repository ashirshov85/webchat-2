# T010: parallel PR verification marker

Temporary marker file for US2 verification (task T010).

Purpose: this change is intentionally trivial and green. It is pushed as a
second pull request while the failing-test pull request is still running,
to demonstrate that parallel pull requests receive independent CI runs
(spec.md US2 acceptance 3).

This file is removed together with the verification branch.
