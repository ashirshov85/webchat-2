# Gradle build cache and outputs are rebuilt inside the Docker build stage
.gradle/
build/
out/

# Local env and logs never enter the image build context
.env
.env.*
*.log

# VCS and image metadata stay out of the build context
.git
.gitignore
.dockerignore
Dockerfile

# IDE and OS artifacts
.idea/
.vscode/
.DS_Store
